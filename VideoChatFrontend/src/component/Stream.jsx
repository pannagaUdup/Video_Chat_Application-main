import React from 'react';
import './css/streamContainer.css'; // Assuming you'll add the related CSS in a file

const Stream = () => {
  return (
    <section id="stream__container">
      <div id="stream__box"></div>
      <div id="streams__container"></div>
    </section>
  );
};

export default Stream;
